# Optional: the client defaults to http://localhost:8080
VEKTOR_URL=

# Optional: the ID (not slug) of the Vektor space to publish.
# If omitted, the loader uses the first space the token can see.
VEKTOR_SPACE_ID=

# Create a viewer access token in Vektor. Not needed for a no-auth local instance.
VEKTOR_ACCESS_TOKEN=
