---
import "../style/main.css";

import Link from "../components/Link.astro";

import { svg } from "svg-sprites:sheet";

export type Props = {
  meta: {
    title: string;
    description?: string;
    image?: string;
  };
};

const props = Astro.props;
---

<script>
  import "../components/SvgIcon.js";
</script>

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <meta name="theme-color" content="#1e1e20" />

    <title>{props.meta?.title}</title>

    {
      props.meta?.description ? (
        <meta name="description" content={props.meta?.description} />
      ) : null
    }

    <meta property="og:title" content={props.meta?.title} />
    <meta property="og:description" content={props.meta?.description} />

    {
      props.meta?.image ? (
        <meta property="og:image" content={Astro.site + props.meta.image} />
      ) : null
    }

    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon" />

    <script defer src="https://abc.luckydye.de/script.js" data-website-id="0be44dad-9f23-410d-ae1f-61b43415cd5d"></script>

    <slot name="head" />
  </head>

  <body>
    <div class="hidden" set:html={await svg()} />

    <main class="overflow-hidden mx-auto px-8">
      <slot name="main" />
    </main>

    <footer class="pb-40 pt-20 mx-auto px-8">
      <div class="container pt-10 pb-20">
        <h2 class="mb-4 text-2xl">Connect</h2>
        <div class="mb-1">
          <Link link="twitter">@tih4v</Link>
        </div>
        <div class="mb-1">
          <Link link="mail">tim.h4vlicek@gmail.com</Link>
        </div>
      </div>
    </footer>
  </body>
</html>
